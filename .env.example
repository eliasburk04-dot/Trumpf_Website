# Copy this file to .env and fill values for real email sending
# The dev server will also create ./outbox/*.eml if SMTP is not configured

# Where to send incoming contact emails
MAIL_TO=eliasburk04@gmail.com
MAIL_TO_NAME=Thomas Burk GmbH

# SMTP settings for sending (use your provider)
SMTP_HOST=smtp.your-provider.tld
SMTP_PORT=587
SMTP_USER=your_mailbox@example.com
SMTP_PASS=your-strong-password
